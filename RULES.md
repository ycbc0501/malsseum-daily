# 절대 규칙 — Saint Seoul (@saintseoul_studio)

**이 파일이 유일한 기준입니다.** 파이프라인을 건드리기 전에 이것부터 읽고, 바꾼 뒤에 이 목록이
전부 그대로인지 확인하십시오. 규칙을 의도적으로 바꾼다면 **같은 커밋에서 이 파일을 고칩니다.**
새 규칙은 여기에 적습니다 — 코드 주석에만 남기면 다음 사람이 못 찾습니다.

이유·역사·측정값은 [CONTENT_RULE.md](CONTENT_RULE.md)에 있습니다. 그건 설명이고, 이건 기준입니다.
둘이 어긋나면 **이 파일이 이깁니다.**

---

## A. 콘텐츠 제작의 법칙 — 이미지와 영상 **둘 다**

**이 네 가지가 법입니다.** 코드에는 `content_law.LAW` 한 벌로만 존재하고, 정지 이미지 프롬프트와
영상 프롬프트가 **둘 다 거기서 가져갑니다.** 프롬프트를 고칠 때는 이 파일을 먼저 보고, 금지 규칙은
프롬프트에 직접 쓰지 말고 `content_law.py`에 적으십시오 — 여러 군데 쓰면 서로 모순됩니다.

**1. 글씨는 나오지 않는다.** 글자·단어·숫자·손글씨·인쇄물·간판·라벨·책등·시계 문자판·워터마크·로고.
   프레임 어디든, 아무리 작아도, 흐려도, 잘려도. **글씨가 있어야 할 자리인데 뭉개진 얼룩만 있어도
   안 됩니다.**

**2. 사람은 나오지 않는다. 사람의 일부도 나오지 않는다.** 얼굴·몸·실루엣·손·팔·다리·발,
   유리나 물에 비친 사람. 멀리 있어도, 초점이 나가도, 뒤돌아 있어도, 화면 끝에 잘려도.
   **아무도 없어야 합니다.** 빈 의자·빈 벤치·주인 없는 신발은 정상입니다 — 부재가 요점입니다.

**3. 실제로 일어날 수 있는 것만 나온다.** 프레임 안의 모든 것은 물리 세계에 실재하는 사물이거나
   실제로 일어나는 현상이어야 하고, 실제로 그러는 대로 행동해야 합니다. 공상과학·환상·초현실 금지.
   **물은 아래로 떨어지고 그릇 밖으로 쏟아지지 않습니다.** 반영은 정확하고, 빛에는 출처가 있고,
   크기와 비례는 사실입니다.

**4. 찍은 것처럼 보여야 한다. 만든 것처럼 보이면 안 된다.** 보는 사람이 **"누가 실제로 가서
   찍었구나"**라고 생각해야 하고, **"편집했구나, 툴로 만들었구나"**라는 생각이 들면 안 됩니다.
   반들거리는 3D·CGI·디지털 페인팅·플라스틱 질감·부자연스러운 완벽한 대칭 금지. 사진 위에 얹은
   판·띠·배경 금지. **화면을 두 구역으로 가르는 직선 경계 금지.** 처음부터 끝까지 한 장의 사진.

**영상은 모든 프레임에 적용됩니다.** 중간에 들어오거나 자라나거나 드러나는 것도 포함입니다 —
정지 이미지가 게이트를 다 통과한 뒤 Veo가 사람을 걸어 들여보낸 게 실제로 있었습니다.

**장면이 모양을 유지해야 합니다.** 프레임 한 장씩 묻는 검사로는 **변형**을 못 잡습니다 —
2026-09-19 고린도전서 15:55 은 모든 프레임이 멀쩡한 사진이었고, 그 사이에서 **울타리가 자리를
옮기고 개수가 바뀌었습니다.** 그래서 묻지 않고 **잽니다**: 클립 첫 프레임과 끝 프레임의
아래쪽 엣지 차이(`make_video.structure_drift`)가 **20.0** 을 넘으면 그 클립은 반려합니다.
실측 근거 — 신고된 릴은 구간별 **34.9·35.5·41.1·42.5**, 불만 없던 릴은 **3.7~12.0** 으로
두 집단이 겹치지 않습니다.

**검사는 정지 이미지뿐 아니라 완성된 영상에도 합니다.** 지금까지 모든 게이트가 Veo에 *들어가는*
프레임만 봤고 *나온* 결과는 아무도 안 봤습니다. 그래서 Veo가 사람을 걸어 들어오게 하고, 물을
거꾸로 쏟고, 위쪽 빈 띠를 줄여 없애는 걸(측정: 0초 21% → 9초 0%) 전부 놓쳤습니다.
이제 클립의 **중간 프레임과 끝 프레임**을 같은 게이트로 봅니다.

**이 일곱 가지는 요청만 하지 않고 반드시 검사합니다.** `fetch_higgsfield._CHECK_PROMPT`의 8개 항목이
매 렌더를 봅니다. 반려되면 **다른 씬으로** 다시 만듭니다 — 같은 씬을 다시 그리면 같은 문제가 또
나옵니다(카페 간판은 몇 번을 그려도 간판입니다).

## B. 말씀

1. **개역개정 원문 그대로.** 자르지 않고, 붙이지 않고, 다듬지 않고, 현대어로 바꾸지 않습니다.
   원문대로 쓰거나, 안 씁니다. (2026-09-06 감사에서 98개 중 27개가 원문이 아니었습니다.)
2. **`verses.json`은 손으로 고치지 않습니다.** `build_verses.py`가 만들고 워크플로가 씁니다.
   손으로 고치는 것이 그 27개를 만든 원인입니다.
3. **같은 구절이 1년 안에 다시 나오지 않습니다.** 캡션이 반복되면 인스타그램이 계정 전체의 도달을
   제한합니다. 풀이 작으면 그 자체가 규칙 위반입니다.
4. **아침에 읽을 수 없는 구절은 넣지 않습니다** — 심판·재앙·저주·전쟁 선언, 원수를 저주하는 기도,
   이야기의 조각, 앞 절을 알아야 뜻이 통하는 것, 종말 예언, 1인칭 탄식, 사람의 틀린 말 인용.
   기준은 하나입니다: **오늘 처음 본 사람이 배경 설명 없이 읽고 힘을 얻는가.**
5. **캡션은 말씀, 출처, 그리고 우리 문장 한 줄입니다.** 해시태그·안내는 여전히 첫 댓글로.

   **2026-09-14 개정.** 그전까지는 "말씀과 출처뿐"이었습니다. 바꾼 이유는 취향이 아니라
   측정입니다 — 인스타그램이 계정을 **퍼온 콘텐츠로 도달 제한**했는데, 플래그된 것은 몇 시간
   전에 우리가 Veo·Higgsfield·Lyria로 **전부 생성한** 게시물이었고, 그 게시물을 눌러도
   **"원본 크리에이터"로 아무 계정도 뜨지 않습니다.** 영상이 매칭된 게 아니라는 뜻입니다.
   남는 건 글자인데, 캡션·자막·첫 댓글이 전부 개역개정 본문 아니면 고정 템플릿이라
   **이 계정의 텍스트 중 우리 것이 한 글자도 없었습니다.**

   - 한 문장, **60자 이내**, 조용한 평서문 (`reflection.py`).
   - **말씀이 먼저고 가장 위입니다.** 한 줄은 출처 **아래**에 옵니다.
   - **파는 말은 여전히 금지입니다.** 2026-08-01에 캡션에서 CTA를 뺀 근거는 그대로 유효합니다 —
     그때 지운 것은 팔로우 유도였지 말씀에 대한 우리 문장이 아니었습니다.
   - **실패하면 조용히 말씀과 출처만** 나갑니다. 캡션 한 줄이 게시를 막을 수 없습니다.
   - 릴과 캐러셀이 **같은 `build_caption()`** 을 씁니다 (0-4).

   **2026-09-17 측정 — 이 한 줄은 퍼온 콘텐츠 판정을 지우지 못했습니다.** 한 줄이 붙어 나간
   게시물 **4건이 4건 모두** 앱의 퍼온 콘텐츠 목록에 올라 있습니다(`metrics.py flagreport`).
   규칙은 그대로 둡니다 — 우리 문장을 한 글자도 안 싣는 상태로 돌아갈 이유는 없고, 이 줄은
   "권리를 소유하지 않은 콘텐츠" 쪽 근거를 줄입니다. 다만 **이것이 원인이었다고 쓰면 거짓입니다.**
   다음 후보는 형식(15초 고정·같은 배치·같은 글꼴)과 개역개정 본문 자체입니다.

## C. 절대 두 번 올리지 않습니다

1. **한 슬롯에 한 게시물.** 릴은 05:00·19:00 KST, 캐러셀은 일요일 11:00 KST.
2. **스케줄 규칙을 믿지 않습니다.** 깃허브는 크론마다 다르게 늦고, **아예 발동 안 하기도 합니다** —
   최근 한 주 저녁 체인은 5개 중 2~4개만 돌았고, 주장 창에 아무도 안 들어온 날은 게시가 빠졌습니다
   (9/13, 9/14). 그래서 "어느 크론이 이 슬롯의 주인인가"를 **추론하지 않습니다.**
   목표 시각이 지난 실행은 **인스타그램에 슬롯이 비었는지 물어보고, 비었으면 채웁니다**
   (`LATE_LIMIT_MIN` 6시간 이내).
2b. **늦은 실행은 증거가 있어야 합니다.** 슬롯 상태는 `filled`/`empty`/**`unknown`** 셋입니다.
   정시 실행은 `unknown`이어도 게시합니다 — 스케줄이 이미 그 슬롯을 그 실행 것으로 정했고, API가
   한 번 흔들렸다고 게시를 거를 이유가 없습니다. **늦은 실행은 스케줄 근거가 없고 증거뿐이라,
   `unknown`이면 물러섭니다.**
2c. 중복 방지의 실제 보장은 `concurrency` 큐와 **게시 직전 재확인**입니다 — 창 계산이 아니라.
3. **게시 직전에 인스타그램에 다시 묻습니다.** 빌드 전이 아니라 빌드 후에.
4. **인스타그램이 진실의 원천입니다.** 로컬 원장이 아니라 계정의 실제 캡션을 보고 고릅니다.
   원장을 잃어도 중복이 불가능해야 합니다.
5. **게시된 글은 기록을 잃지 않습니다.** 원장 저장은 `if: always()`, 5회 재시도, 텍스트 rebase가
   아니라 `ledger_merge.py`의 의미 기반 병합.
6. **가장 오래 안 나온 것을 고릅니다** — 파일 순서가 아니라 `metrics.json`의 마지막 게시일 기준.
7. **한 번이라도 게시된 구절은 후보에서 빠집니다** — "오래됐다"는 자격이 아닙니다. `used_verses`
   원장은 매 실행마다 **두 곳에서** 복원합니다: `published_refs()`(인스타그램, 최근 25건)와
   `last_published()`(`metrics.json`, 전 기간). 어느 하나도 단독으로는 충분하지 않습니다 —
   2026-09-14에 원장에는 62개가 있었는데 실제 게시는 166건·고유 구절 119개였고, **이미 나간
   구절 49개가 후보 풀에 그대로 있었습니다.** 같은 날 계정은 여전히 퍼온 콘텐츠 도달 제한
   상태였고 원장에는 **두 번 이상 나간 구절이 47개** 있었습니다. 캐러셀도 같은 규칙을 씁니다.

## D. 겹치지 않아야 하는 것들

돌아가는 모든 자원은 no-repeat 원장을 가집니다 (`state.json`).
구절 · 이미지 씬 · 영상 클립 · 음악 · 사진 — 하나라도 원장 없이 돌면 반드시 겹칩니다.

**씬 계열을 추가하면 같은 변경에서:**
- `INTERIOR_CATS`에 실내 여부를 등록합니다 (안 하면 방 위에 하늘이 생깁니다)
- 게이트가 그 계열의 실패 유형을 설명하는지 확인합니다

## E. 영상·음악

1. **모든 게시물은 영상**입니다. 정지 이미지는 폴백일 때만.
2. **실시간 속도, 잔잔하게.** 루프 금지, 역재생 금지.
   **속도는 1초 간격으로 잽니다** (`make_video.motion_score`). 인접 프레임(1/30초) 비교는 눈이
   느끼는 속도와 무관합니다 — 2026-09-08까지 그렇게 재서, 게시된 5개가 한계 2.0에 0.21~0.76으로
   전부 통과했고 게이트는 **한 번도 작동한 적이 없었습니다.** 1초 간격으로 재면 같은 영상이
   1.65~4.52입니다.
3. **재시도는 같은 요청을 다시 뽑는 게 아니라 요구를 강화합니다** (`fetch_veo.CALMER`).
   같은 프롬프트로 3번 뽑으면 같은 분포에서 3개를 꺼내는 것뿐입니다 — Veo가 이 이미지의 하늘을
   원래 빠르게 만든다면 3개 다 빠릅니다. 2회차는 "움직임을 1/4로, 하늘은 완전 정지",
   3회차는 "거의 정지 사진, 자세히 봐야 움직이는지 알 정도".
4. **게이트는 반려가 아니라 선택입니다.** 목표(1.5 / 0.35)를 넘으면 반려하는 게 아니라
   **3회 생성해서 가장 잔잔한 것**을 씁니다. 정지 이미지 폴백은 구제 불능(8.0 / 4.0)일 때만 —
   조금 활발한 영상보다 정지 사진이 더 나쁜 답입니다.
5. **연결 세그먼트도 같은 대우를 받습니다.** 한 번 만들고 탈락시키면 릴이 15.3초에서 7.7초로
   짧아지고, 짧은 릴은 말씀을 읽기 전에 루프됩니다.
6. **음악은 매번 새로 만듭니다** (Lyria). 같은 음악 두 번 금지.
7. **영상이 음악보다 길면 안 됩니다**
8. **실제로 나간 영상의 모션 점수를 기록합니다** (`_meta.json`의 `motion`, `motion_sky`,
   `motion_attempts`). 눈으로 매번 다시 판단하지 않기 위해서입니다. — 음악이 루프되면 이음매가 드러납니다.

## F. 글씨(오버레이)가 읽혀야 합니다

1. 말씀은 프레임 **위쪽**, 배경의 빈 곳에 놓입니다.
2. 배경 위쪽 절반은 **비어 있고 단순**해야 합니다 — 다만 **그 장면의 실제 일부**여야 합니다.
   "하나의 평평하고 균일한 톤, 질감 없음"을 요구하면 모델이 **사진 위에 칠판을 한 장 얹습니다.**
   9/14·9/17 게시물이 정확히 그랬습니다(위쪽 21%가 평평한 판, y=127에 직선 경계).
   **가로 직선 경계로 화면이 두 구역으로 갈리면 안 됩니다.**
2b. **글자 크기는 두 가지뿐입니다** — 44px(릴)와 35px. 두 줄에 안 들어가면 작은 쪽으로
   내려갑니다. 애매하게 세 줄로 쪼개지 않습니다. (694절 중 657개가 44px, 37개가 35px, 3줄 0개)
3. **요청이 아니라 측정합니다.** `text_area_ok()`가 실제 글자 마스크 아래 대비를 재고, 실패하면
   다시 만듭니다.
4. **줄바꿈이 구문을 끊지 않습니다** — 동사구, 수식-피수식을 갈라놓지 않습니다.

## G-1. 알림은 문제가 생겼을 때만 — **이 브리지 봇 방으로**
`TELEGRAM_BOT_TOKEN`은 브리지 봇이고 `TELEGRAM_CHAT_ID`는 계정 주인의 개인 채팅입니다.
전용 알람봇으로도 보내봤지만 **두 번 연속으로, 감시가 놓친 게시를 정확히 잡아 보냈는데도 계정
주인은 직접 발견했습니다** — 안 보는 방으로 갔기 때문입니다. 읽히지 않는 알림은 없는 알림입니다.

**감수한 비용:** 이 토큰은 계정 주인의 맥에서 Claude Code를 구동하는 봇입니다. 저장소 시크릿이
새면 알림 채널이 아니라 그 봇이 넘어갑니다. **의도적인 교환이고 2026-09-13에 계정 주인이
결정했습니다.** 되돌리려면 시크릿 두 개를 알람봇 것으로 되돌리면 됩니다.

**평소엔 조용합니다.** 하루 두 번 "올라갔습니다"는 소음이고, 소음은 곧 안 보게 됩니다.
말을 거는 경우는 세 가지뿐이고, 한 사건당 한 번입니다(5일 쿨다운):

- **좋아요 급락** — 최근 4일 중앙값이 직전 18일의 절반 이하 (표본 5개 이상일 때만).
  3일/3개로 잡으면 0 하나에 중앙값이 흔들려 멀쩡한 날에도 울립니다
- **반응 없음** — 4개 연속 좋아요 1개 이하 (도달 제한 의심)
- **슬롯 누락** — 05:00 / 19:00 KST 게시가 **2시간 넘게** 안 올라옴. 슬롯마다 따로 봅니다:
  하루 두 번 올리는 계정에서 "30시간 무소식"은 아침 하나가 통째로 사라져도 조용합니다.
  워크플로가 *아예 안 도는* 경우는 실패 알림도 안 옵니다 (8/27에 실제로 그랬습니다).
  판단은 **인스타그램에 직접 물어서** 합니다 — 원장 저장이 실패한 게시를 "안 올라왔다"고
  보고하면 알림 자체의 신뢰가 무너집니다
- 그리고 게시 **실패** 시 실행 링크 — **단, 실패한 것은 실행이지 슬롯이 아닙니다.**
  알림을 보내기 전에 **인스타그램에 슬롯이 비었는지 묻고**, 이미 채워졌으면 **아무 말도 하지
  않습니다.** 체인은 원래 재시도하고 물러섭니다. 한 슬롯당 **최대 한 번**만 말합니다.
  **그리고 그 슬롯이 나중에 채워지면 `✅ 해결됐습니다`를 보냅니다.**
  2026-09-19에 ❌ 다섯 개가 갔고 19:00 게시는 19:18에 올라갔는데, 계정 주인은 알림만 보고
  **저녁 게시가 또 실패한 줄 알았습니다.** 마지막 말이 항상 ❌ 인 채널은 믿을 수 없는 채널입니다.

**8월 붕괴로 백테스트했습니다** — 정상 기간(8/20~8/28) 오탐 0건, 8/31 최초 발동.
눈으로 알아챈 9/6보다 **6일 빠릅니다.** 파라미터를 바꿀 때마다 이 백테스트를 다시 돌리십시오.

알림은 절대 게시를 실패시키지 않습니다 (`continue-on-error`, 설정 없으면 조용히 통과) —
알림이 자기가 보고하려던 장애를 만들면 안 됩니다.

## G. 이 규칙들이 지켜지는지 확인하는 법

```
bash verify.sh     # 이거 하나. 푸시할 때마다 CI 가 똑같이 이걸 돌립니다
```

컴파일 · **교차 참조**(`audit_refs.py`) · 규칙 · 워크플로 env 감사 · **테스트 전부**를 돌립니다.
테스트와 감사는 **glob 으로 찾습니다** — 새로 만들면 어디에 등록하지 않아도 자동으로 포함됩니다.
검사를 만들어놓고 연결을 안 해서 며칠씩 깨진 채로 있던 게 반복됐습니다.

**`audit_refs.py`가 특히 중요합니다.** 파이썬은 `모듈.이름`을 실행 시점에 찾기 때문에, 상수를
지워도 컴파일은 통과합니다. 하루에 세 번(`NOTEXT`·`COMPOSE`·`CALMER`) 그렇게 지웠고 두 번은
커밋까지 갔습니다.

적어만 두고 강제하지 않는 규칙은 없느니만 못합니다 — 읽는 사람에게는 보장으로 보이니까요.
`check_rules.py`가 이 파일의 각 항목이 코드에 실제로 있는지 검사합니다. **여기에 규칙을 추가하면
그 검사도 같이 추가하십시오.**

## H. 일하는 방식
0-1. **워크플로가 코드에 필요한 env 를 넘기는지 기계로 검사합니다** (`audit_env.py`).
   이게 이 저장소에서 가장 비싼 버그 유형입니다 — **조용히** 실패하기 때문입니다. 토큰이 없으면
   `None`이 넘어가고, API가 400을 주고, 넓은 `except`가 잡고, 가드가 폴백하고, 로그엔 아무것도
   안 남습니다. `daily_post`와 `carousel_post`의 중복 방지 가드는 **평생 죽어 있었습니다.**
0-2. **"검증 못 함"을 "통과"로 적지 않습니다.** 감사 도구가 못 돌면 `CANNOT VERIFY`로 실패
   처리합니다. 확인 안 된 가드는 확인된 가드가 아닙니다.
0-3. **라이브러리 코드는 `SystemExit`을 던지지 않습니다.** 호출부는 `except Exception`으로
   감싸는데 `SystemExit`은 `Exception`이 아닙니다. "어떤 에러든 통과시킨다"고 문서화된 게이트를
   그대로 뚫고 나가 게시를 죽입니다. `SystemExit`은 `main()` 전용입니다.
0-4. **원장은 한 구현만 씁니다** (`save_ledger.sh`). 구현이 여러 벌이면 한 벌만 고쳐지고
   나머지는 옛 버그를 그대로 안고 갑니다 — 실제로 8/26 버그가 세 워크플로에 2주 더 남아 있었습니다.
0-5. **금지된 동작은 코드에서 지웁니다.** 아무도 안 부르는 `make_slowmo`·`make_boomerang`이
   남아 있었습니다. 규칙이 금지한 동작의 완성된 구현은 문서가 아니라 장전된 총입니다.
0. **에러를 삼키지 말고 API가 준 이유를 남깁니다.** `urllib`은 "HTTP Error 400"만 주고 본문을
   버리는데, 이유는 본문에 있습니다. 몇 주 동안 로그에 `slot check failed (HTTP Error 400)`만
   찍혀서 손댈 단서가 없었습니다. 폴백은 조용해도 되지만 **로그는 조용하면 안 됩니다.**
0b. **일시적 실패는 재시도합니다.** 인스타그램 미디어 처리는 멀쩡한 영상도 가끔 거부합니다
   (`2207076`). 9/9 저녁 릴은 다 만들어놓고 게이트를 다 통과한 뒤 여기서 **한 번에** 죽었습니다.
   재시도는 컨테이너를 새로 만듭니다 — 실패한 컨테이너는 되살릴 수 없습니다.

1. **배포하고 실제로 돌려보기 전까지 "고쳤다"고 하지 않습니다.** 2026-09-06에 크론 중복 방지를
   "전수 검증"으로 보고했고 그날 밤 같은 글이 두 번 나갔습니다. 검증의 전제가 틀렸습니다.
2. **넓은 `except`는 버그를 숨깁니다.** 실패하면 무엇이 왜 실패했는지 출력합니다.
3. **확인 코드가 고장 났을 때 글을 삼키지 않습니다.** 검사 실패는 경고를 남기고 게시를 진행합니다.
   단, **중복은 예외** — 중복이 의심되면 멈춥니다.
4. **측정할 수 있으면 논쟁하지 않습니다** (`metrics.json`).
