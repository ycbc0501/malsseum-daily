# 조회수 반등 — 기다림은 이미 실패했다 (2026-09-14)

`notes/reach-collapse-2026-08-27.md` 에서 내린 결론은 "원인은 제거했으니 며칠~2주 기다리면
도달이 회복된다" 였다. **18일이 지났고 회복은 없다.** 이 문서는 그 판정과, 기다림 말고
남아 있는 수단을 적는다.

## 1. 붕괴 폭이 부풀려져 있었다 — 그리고 미약한 반등이 이미 있다

**먼저 정정한다.** 이 문서를 쓰기 시작할 때 읽은 숫자는 틀린 숫자였다. §2b 의 버그 때문에
최근 게시물은 **게시 다음 날 숫자에서 얼어붙어** 있었고, 붕괴 전 게시물은 한참 뒤에
백필되어 충분히 익은 숫자였다. **최근을 낮게, 과거를 높게 읽는 편향**이다.

오늘 원장을 풀어주자 48건이 갱신됐다. 몇 개만:

```
09-08 전도서 4:6        1 → 10
09-11 이사야 14:7       2 →  8
09-12 요한복음 6:20     2 →  9
09-01 마태복음 11:28    2 →  9
```

주 단위 평균(좋아요+댓글), **정정 후**:

```
W32  n=15  14.2
W33  n=15  17.2
W34  n=15  22.3     ← 붕괴 직전 주
W35  n=13  12.8     ← 08-27 절벽
W36  n=14   7.2     ← 바닥
W37  n=12   9.2     ← +28%
W38  n= 1   6.0     (오늘 게시 1건, 아직 안 익음)
```

두 가지가 달라진다:

1. **절벽은 여전히 실재한다** — 22.3 → 12.8 → 7.2. 붕괴 자체는 정정 후에도 남는다.
   다만 "15 → 1" 이 아니라 **"22 → 7"** 이다. 1/15 이 아니라 1/3.
2. **W36 → W37 에서 처음으로 올라왔다** (7.2 → 9.2, +28%). 그리고 이 비교는
   회복에 **불리한** 쪽으로 편향돼 있다 — W37 게시물이 W36 보다 **덜 익었는데도** 더 높다.
   둘 다 오늘 같이 갱신됐으므로 측정 시점은 동일하다.

> 판정: **"반등이 전혀 없다"는 내 첫 판단은 틀렸다.** 미약하지만 방향은 위다.
> 그렇다고 기다림이 정답이라는 뜻은 아니다 — 3주 만에 22 의 40% 지점이고,
> 이 속도면 원래 수준 복귀까지 한참이다. 아래 수단은 그대로 유효하다.

팔로워는 08-06 186명 → 09-14 198명 (+12). 죽지는 않았고, 늘지도 않는다.

## 2. 우리는 아직도 눈이 멀어 있었다 (오늘 고침)

166건 전부 `views` 가 비어 있다. 좋아요·댓글만 있다. **"조회수 반등"을 이야기하면서
조회수를 한 번도 기록한 적이 없다.**

2026-09-09 에 고쳤다고 적어둔 커밋(`1861089`)이 **푸시되지 않았다.** 로컬에만 있었고,
그 사이 랩탑 세션이 32커밋을 올리면서 갈라졌다. 오늘 `9d7eba6` 로 다시 얹었다.

그리고 그 수정에도 구멍이 있었다 (`404ed68` 로 막음):

- 기존 수정은 **모든 티어가 거부됐을 때만** 지표를 하나씩 따로 물어봤다.
- 그런데 티어 3·4 는 `views` 를 아예 안 물어본다. 토큰이 티어 3만 통과하면
  **성공적으로 reach 를 받고 조기 반환** → views 는 영원히 빈칸.
- 이제 **답이 온 티어에 views 가 없으면 views 만 따로 한 번 더** 물어본다.

### 2b. 그리고 더 큰 결함 — 숫자가 게시 다음 날에 얼어붙었다

`insights.py` 의 `if not got:` — 이미 무언가 기록된 항목은 **다시 물어보지 않았다.**
릴스는 일주일 넘게 숫자가 쌓이고, 그래서 `metrics.MATURE_DAYS` 가 14일인데,
정작 원장은 **하루치 스냅샷**만 들고 있었다.

좋아요·댓글은 이미 받아오는 목록 응답에 딸려 온다(추가 호출 0). 이제 매번 갱신한다.
스코프가 필요한 지표(views·reach)는 **아직 없고 아직 익는 중인** 게시물에만 물어보고,
빈 응답 3번이면 그 실행은 포기한다 — 토큰에 권한이 없다는 걸 90번 확인해봐야 소용없고,
호출을 태우는 것이 중복 방지 가드를 멀게 했던 400 의 시작이었다.

**실행으로 확인함** (`gh run 34801920728`): 48건 갱신, 서킷 브레이커 작동.

### 2c. views 의 진짜 차단 사유가 로그에 찍혔다

```
insights tier reach..(9) rejected: HTTP 400:
  (#10) Application does not have permission for this action (code=10)
```

토큰이 아니라 **앱에 권한이 없다.** `IG_ACCESS_TOKEN`(페이스북 로그인 시스템 유저 토큰)은
`instagram_manage_insights` 를 못 받는다 — 비즈니스 관리자에서 SMS 2FA 를 통과해야 하는데
그 번호로 메타 코드가 안 온다. 그래서 `ig_login.py` 가 존재한다: **인스타그램 로그인**으로
읽기 전용 토큰을 따로 발급받는 우회로다.

**`IG_INSIGHTS_TOKEN` 시크릿은 아직 비어 있다. 그것이 채워지기 전에는 조회수를 영영
기록할 수 없다.** 코드는 전부 준비됐고, 남은 건 발급 한 번이다:

```
export IG_APP_ID=... IG_APP_SECRET=...     # Meta 앱 → Instagram → API setup
python3 ig_login.py --auth-url             # 뜬 URL 로 로그인·승인
python3 ig_login.py --code 'AQB...'        # 리다이렉트의 code= 붙여넣기 → 60일 토큰
# 나온 토큰을 저장소 시크릿 IG_INSIGHTS_TOKEN 에 넣는다
```

## 2d. 계정 상태 확인 결과 — **제재는 아직 살아 있다** (09-14 확인)

계정 주인이 앱에서 확인한 화면:

> **회원님의 계정과 콘텐츠가 팔로워가 아닌 사람에게 추천될 수 없습니다**
> 취할 수 있는 조치 → **회원님의 오리지널 콘텐츠가 아닌 콘텐츠  10 ›**

두 가지가 확정된다:

1. **18일이 지났는데 제한이 안 풀렸다.** §1 의 미약한 반등(W36 7.2 → W37 9.2)은
   제재가 풀려서가 아니라 **팔로워 안에서만** 일어난 것이다. 비팔로워 추천은 여전히 0이다.
2. **09-08 의 아카이브는 해결로 카운트되지 않았다.** 숫자가 그대로 10 이다.
   아카이브는 프로필에서 숨길 뿐 게시물은 남는다 → **삭제해야 한다.**

## 2e. 그리고 문제는 10건이 아니었다 — **중복 구절 47개, 게시물 100건**

원장 전수 조사 결과:

```
중복 구절 47개 / 해당 게시물 100건  (전체 166건 중)
■ 이사야 53:5   3회  (06-29, 07-05, 08-31)
■ 마태복음 11:28 3회  (06-29 두 번, 09-01)
■ 잠언 18:10    3회  (08-09, 08-27, 08-28)
■ 잠언 27:17    3회  (08-11, 08-29, 08-30)
 … 43개 더
```

인스타그램이 플래그한 10건은 **빙산의 일각**이다. 08-15~08-21 원장 공백 때문이라고
적었던 진단은 **너무 좁았다** — 중복은 6월부터 전 기간에 걸쳐 있었다.

### 왜 — 원장이 역사를 잃고 있었다

| | |
|---|---|
| `state.json` 의 `used_verses` | **62개** |
| 실제 게시 | **166건** |
| 고유 구절 | **119개** |

**이미 나간 구절 49개가 후보 풀에 그대로 남아 있었다.** 옛 사이클 리셋(`used_verses = []`)이
원장을 비웠고 아무도 다시 채우지 않았다. 인스타그램에 직접 묻는 `published_refs()` 는
**최근 25건**만 본다 — 그 바깥의 구절은 보이지 않는다.

그리고 선택 로직은 이미 나간 구절을 **제외하지 않고 "가장 오래된 것 우선"으로 정렬만** 했다.
한 번도 안 나간 구절이 있으면 그게 먼저 뽑히므로 대개는 괜찮지만, **"오래됐다"는 자격이 아니다.**

### 고친 것 (RULES C-7 신설, `check_rules.py` 검사 3개)

- `used_verses` 를 매 실행마다 **인스타그램 + `metrics.json` 양쪽**에서 복원한다.
  어느 하나도 단독으로는 불충분하다 (하나는 최근 25건만, 하나는 다른 잡이 쓴다).
- 캐러셀도 **한 번이라도 나간 구절은 후보에서 제외**한다 (정렬이 아니라 제외).
- `state.json` 을 지금 즉시 복구했다: **62 → 111**. 후보 풀 650/694.
- 검사 `C7 the ledger knows every verse already published` 가
  `metrics.json` 의 모든 ref 가 원장에 있는지 **매 푸시마다** 대조한다.

## 3. 기다림 말고 실제로 있는 수단

증거의 강도 순으로 적는다. **한 번에 하나씩** 바꿔야 무엇이 효과였는지 알 수 있다.

### A. 계정 상태를 직접 확인한다 — 30초, 사람만 할 수 있다 (최우선)

인스타 앱 → 설정 → **계정 상태(Account Status)**.

- 여전히 "추천에서 제외됨 / 퍼온 콘텐츠" 라고 떠 있으면 → **제재가 안 풀린 것**이고,
  거기서 **검토 요청(Request Review)** 을 누를 수 있다. 이건 기다림이 아니라 행동이다.
- 아무 표시도 없으면 → 제재는 풀렸는데 숫자가 안 돌아온 것이고, 원인은 다른 데 있다.

**이 한 번의 확인이 아래 전부의 전제를 가른다.** 우리 쪽 API 로는 이 상태를 못 읽는다.

### B. 플래그된 게시물은 보관이 아니라 삭제

09-08 에 10건을 **아카이브**했다. 아카이브는 프로필에서 숨길 뿐 게시물은 남는다.
중복 게시물이 제재 근거였다면 **남아 있는 한 근거도 남아 있다.** 삭제가 맞다.

### C. 게시 빈도 — 하루 2회는 197명짜리 계정에 과하다

붕괴 전에도 주 13~15건이었으니 빈도가 **원인은 아니다.** 다만 도달이 눌린 상태에서는
같은 관객을 두 번 나눠 먹어 **게시물당 반응률**이 더 떨어지고, 반응률은 추천 알고리즘이
보는 신호다. 2주간 **하루 1회(저녁)** 로 줄이고 게시물당 조회수를 비교해볼 값어치가 있다.

### D. 유입구가 추천 하나뿐이다 — 이게 구조적 문제

현재 이 계정이 새 사람에게 닿는 경로:

| 경로 | 상태 |
|---|---|
| 릴스 추천 | **제재로 막힘** |
| 해시태그 | 스스로 포기 (`hashtags.py`: "해시태그는 도달을 만들지 않는다" — 5개 고정) |
| 트렌딩 오디오 | 없음 (Lyria 자체 생성 음원 → 오디오 페이지 유입 0) |
| 협업·리믹스·스레드 | 없음 |

**즉 추천이 막히면 유입이 0이 된다.** 제재가 풀리기를 기다리는 것 말고 방법이 없는
구조를 우리가 직접 만들었다. 추천 바깥의 유입구를 **하나라도** 만들어야 한다:

- **스레드(Threads) 교차 게시** — 메타 계열이라 프로필 방문으로 직결되고, 텍스트 말씀은
  스레드 포맷에 그대로 맞는다. 자동화 비용도 가장 낮다.
- 트렌딩 오디오를 쓰는 실험 — 다만 RULES 의 음악 규칙과 충돌하므로 규칙 개정이 먼저다.

### E. 콘텐츠에서 숫자가 말해주는 것 (붕괴 전 132건 기준)

| | |
|---|---|
| 테마 최고 | 감사 20.1 · 평안 18.4 · 은혜 18.3 |
| 테마 최저 | **위로 8.2** (n=13) — 다른 테마의 절반 |
| 포맷 | REELS 17.2 > FEED 9.2 |
| 시간대 | 20~21시 21.9 (n=8) > 19시 17.2 (n=42) > 05시 15.9 (n=38) |

- **위로 테마는 재검토 대상이다.** 표본 13건으로 일관되게 절반이다.
- 20~21시가 19시보다 높지만 n=8 이라 **아직 근거가 약하다.** 조회수가 기록되기 시작하면
  다시 본다.
- 붕괴 *후* 에는 FEED 7.7(n=3) > REELS 4.3(n=31) 로 뒤집힌다. n=3 이라 결론은 못 내지만,
  **릴스만 선택적으로 눌렸다**는 가설과 방향이 맞는다. 퍼온 콘텐츠 제재는 릴스 추천을
  때리는 제재이므로 앞뒤가 맞는다. → **캐러셀/피드 비중을 늘리는 실험**의 근거가 된다.

## 4. 하지 말아야 할 것

붕괴 이후 구절·씬·모션·CTA·게이트를 전부 바꿨는데 **그중 무엇도 이번 급락의 원인이
아니었다.** 여러 개를 동시에 바꾸면 반등이 와도 무엇 덕인지 영영 모른다.
**한 번에 하나, 2주, 게시물당 조회수로 판정.**

## 5. 다음 사람이 할 일

1. [x] **계정 상태 확인** — 09-14 완료. **제재 살아 있음** (§2d)
2. [ ] **10건을 아카이브가 아니라 삭제** — 계정 상태 → "오리지널 콘텐츠가 아닌 콘텐츠 10" →
       목록의 게시물을 **삭제**. 그다음 **검토 요청**. 순서가 중요하다: 근거가 남아 있는 채로
       검토를 요청하면 그대로 유지될 가능성이 높다.
       (우리 쪽 중복 목록은 §2e 에 전부 있다 — 링크까지 원장에서 뽑아 놓았다)
3. [ ] **`IG_INSIGHTS_TOKEN` 발급** (§2c) — 이게 없으면 조회수는 영영 안 들어온다.
       60일마다 갱신해야 하므로, 만료 알림을 `watch.py` 에 붙이는 것도 같이 볼 것
4. [ ] 위 실험 중 **하나만** 골라 2주 (C 또는 D 또는 E)
5. [ ] 다음 주 이 파일의 §1 표를 다시 계산할 것. W37 → W38 도 올라가면 회복 확정이다.
       이제 원장이 안 얼어붙으므로 그 비교가 처음으로 정직해진다

## 부록 — 오늘 한 일 (커밋)

| | |
|---|---|
| `9d7eba6` | 09-09 에 만들고 **푸시 안 된 채 방치된** 조회수 수집 수정을 되살려 얹음 |
| `404ed68` | 답이 온 티어에 views 가 없으면 views 만 따로 물어봄 |
| `0d22e6e` | 게시 다음 날에 얼어붙던 원장을 풀고, 서킷 브레이커 추가 |

`backup/pre-rebase-6eb3e63` 브랜치에 09-09 로컬 작업 원본이 남아 있다. 그 안의
`alert.py` 는 **의도적으로 버렸다** — 랩탑 세션이 `watch.py` 로 같은 일을 이미 배포했고,
RULES 0-4 가 원장·알림의 구현을 한 벌로 유지하라고 한다. 다만 `watch.py` 의 판정식은
계정 주인이 지정한 식(n 대비 n+1·n+2 절반)이 **아니라** 중앙값 기반이다. 그리고 지금은
views 를 안 본다 — 토큰이 생겨 조회수가 들어오기 시작하면 **views 를 두 번째 신호로
`watch.py` 에 넣어야 한다.** 그게 계정 주인이 원래 요청한 것에 가장 가깝다.
