name: Weekly 말씀 carousel

# Sunday ~11:00 KST (Sun 02:00 UTC). A 3-slide carousel: 말씀 → 묵상 → 기도.
#
# Was Sat 19:00 UTC, which landed the carousel at 04:15-06:22 KST Sunday — minutes from the 05:00
# KST reel. Two posts of ours in the same few minutes split their own reach and read as a burst,
# and on 08-22 and 08-29 the carousel also picked the verse the reel had just used. The verse
# collision is fixed in carousel_post.py; this moves the two posts apart in time as well.
on:
  schedule:
    - cron: "0 2 * * 0"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  carousel:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install pillow kiwipiepy

      - name: Build the 3 slides
        run: python carousel_post.py --emit

      - name: Host slides on a GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view media >/dev/null 2>&1 || gh release create media --title media --notes "hosted media"
          URLS=""
          while read -r rel; do
            [ -z "$rel" ] && continue
            gh release upload media "$rel" --clobber
            URLS="$URLS,https://github.com/${GITHUB_REPOSITORY}/releases/download/media/$(basename "$rel")"
          done < output/_carousel.txt
          echo "URLS=${URLS#,}" >> "$GITHUB_ENV"

      - name: Publish carousel to Instagram
        env:
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: |
          echo "URLS: $URLS"
          python post_instagram.py --carousel "$URLS" \
            --caption-text "$(cat output/_caption.txt)" --comment "$(cat output/_comment.txt)" --story
